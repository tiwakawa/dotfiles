== my dotfiles

=== make command
  make
  .zshrc, .vimrc, .vimのシンボリックリンクを作成

  make zsh
  .zshrcのシンボリックリンクを作成

  make vim
  .vimrc, .vimのシンボリックリンクを作成

  make clean
  .zshrc, .vimrc, .vimのシンボリックリンクを削除

== memo
  git clone http://github.com/gmarik/vundle.git ~/.vim/vundle.git
